export enum UserType {
  CLIENT = 'Client',
  DEVELOPER = 'Developer',
  PM = 'Project Manager',
}

export enum QueryKeys {
  USER_PROFILE = 'USER_PROFILE',
  PROJECTS = 'PROJECTS',
  PROJECT = 'PROJECT',
  TASKS = 'TASKS',
}
